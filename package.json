{
	"name": "biometric-diary",
	"version": "0.0.0",
	"description": "A note taking app secured with biometric typing authentication.",
	"engines": {
		"node": "8.9.1"
	},
	"main": "index.js",
	"scripts": {
		"start": "node build/index.js",
		"watch": "nodemon build/index.js",
		"build": "npm run compile-ts-server && npm run compile-ts-client && npm run compile-sass && npm run copy-assets",
		"compile-ts-server": "tslint -p tsconfig.server.json && tsc -p tsconfig.server.json",
		"compile-ts-client": "tslint -p tsconfig.client.json && tsc -p tsconfig.client.json",
		"compile-sass": "node-sass src/stylesheets/ -o build/stylesheets/",
		"copy-assets": "cpx \"src/**/*.{js,ejs,svg,ico,env}\" ./build",
		"test": "concurrently \"npm:compile-ts-server-w\" \"npm:compile-ts-client-w\" \"npm:compile-sass-w\" \"npm:copy-assets-w\"",
		"compile-ts-server-w": "tsc -w -p tsconfig.server.json",
		"compile-ts-client-w": "tsc -w -p tsconfig.client.json",
		"compile-sass-w": "node-sass src/stylesheets/ -o build/stylesheets/ && node-sass -r -w src/stylesheets/ -o build/stylesheets/",
		"copy-assets-w": "cpx \"src/**/*.{js,ejs,svg,ico,env}\" ./build -w",
		"heroku-postbuild": "npm run build"
	},
	"dependencies": {
		"cpx": "^1.5.0",
		"dotenv": "^6.1.0",
		"dtsmake": "0.0.10",
		"ejs": "^2.5.6",
		"express": "^4.16.4",
		"node-sass": "^4.11.0",
		"serve-favicon": "^2.5.0",
		"typingdnaclient": "^1.0.0"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/volleio/Biometric-Diary"
	},
	"keywords": [
		"node",
		"heroku",
		"express"
	],
	"license": "MIT",
	"devDependencies": {
		"@types/dotenv": "^4.0.3",
		"@types/express": "^4.16.0",
		"@types/node": "^10.12.10",
		"@types/serve-favicon": "^2.2.30",
		"concurrently": "^4.1.0",
		"nodemon": "^1.18.7",
		"tslint": "^5.11.0",
		"typescript": "^3.1.6"
	}
}
